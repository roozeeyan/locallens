name: Fetch Wikipedia Descriptions

on:
  workflow_dispatch:
    inputs:
      city:
        description: 'City: yerevan / bangkok / samui / phangan / hoian / bali / danang / singapore (empty = all)'
        required: false
        default: 'yerevan'
      overwrite:
        description: 'Re-fetch even if description already exists'
        required: false
        default: false
        type: boolean

jobs:
  fetch:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: pip install requests

      - name: Fetch descriptions from Wikipedia
        env:
          GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITHUB_REPOSITORY: ${{ github.repository }}
        run: |
          OVERWRITE_FLAG=""
          if [ "${{ inputs.overwrite }}" = "true" ]; then
            OVERWRITE_FLAG="--overwrite"
          fi
          python scripts/fetch_wiki_descriptions.py \
            --city "${{ inputs.city }}" \
            $OVERWRITE_FLAG

      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: descriptions-json
          path: src/descriptions.json
          retention-days: 90
